link-shrink.duckdns.org {
    # Route Grafana to the sub-path
    handle_path /grafana/* {
        reverse_proxy grafana:3000
    }
    
    # Route all other traffic to the main app
    handle {
        reverse_proxy app:5000
    }
}
